<Window x:Class="LilAgents.Windows.UI.BubbleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        IsHitTestVisible="False"
        SizeToContent="Manual"
        Width="80"
        Height="26"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Grid>
        <Border x:Name="BubbleBorder"
                CornerRadius="10"
                Padding="0"
                BorderThickness="1"
                Width="80"
                Height="26">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="10" Opacity="0.32" Direction="270"/>
            </Border.Effect>
            <TextBlock x:Name="BubbleText"
                       FontSize="12"
                       TextWrapping="NoWrap"
                       TextTrimming="None"
                       Width="80"
                       Height="26"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
